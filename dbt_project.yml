name: dbt_crm
version: '1.0'
config-version: 2

profile: my_project

model-paths: ["models"]

models:
  dbt_crm:

    staging:
      +materialized: view
      +schema: staging
      +tags: ["staging"]

    intermediate:
      +materialized: view
      +schema: intermediate
      +tags: ["intermediate"]

    data_products:
      +materialized: table
      +schema: data_products
      +tags: ["data_product"]